@model FurnitureShopApp.DAL.Models.Storage

@{
    ViewData["Title"] = "Список складів";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Пошук</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Search">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StorageId" class="control-label">Склад</label>
                <select asp-for="StorageId" class="form-control selectpicker" data-live-search="true" asp-items="ViewBag.StorageId">
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Підтвердити пошук" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Повернутись до списку</a>
</div>

