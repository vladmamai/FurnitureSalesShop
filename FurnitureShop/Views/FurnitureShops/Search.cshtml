@model FurnitureShopApp.DAL.Models.FurnitureShop

@{
    ViewData["Title"] = "Перелік магазинів";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Пошук</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Search">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ShopId" class="control-label">Магазин</label>
                <select asp-for="ShopId" class="form-control selectpicker" data-live-search="true" asp-items="ViewBag.ShopId">
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Підтвердити пошук" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Повернутись до списку</a>
</div>

